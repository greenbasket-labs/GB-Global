import {redirect} from 'next/navigation';import {getSessionUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';
export default async function Company(){const u=await getSessionUser();if(!u||u.role!=='COMPANY_ADMIN')redirect('/login');const [clients,services,partners,apps,tickets]=await Promise.all([prisma.user.count({where:{role:'CLIENT'}}),prisma.service.count({where:{active:true}}),prisma.partnerProfile.count(),prisma.application.count(),prisma.supportTicket.count({where:{status:{in:['OPEN','IN_PROGRESS']}}})]);return <main className="dashboard"><div className="pill">Company Control</div><h1>Green Basket Control Center</h1><div className="stats"><div className="stat">Clients<strong>{clients}</strong></div><div className="stat">Services<strong>{services}</strong></div><div className="stat">Partners<strong>{partners}</strong></div><div className="stat">Applications<strong>{apps}</strong></div></div><div className="grid"><div className="card"><h3>Support</h3><p className="muted">{tickets} open or active tickets.</p></div><div className="card"><h3>Operations</h3><p className="muted">Manage clients, services, partner work and applications from the company side.</p></div></div></main>}
