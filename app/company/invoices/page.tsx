import {redirect} from 'next/navigation';
import Link from 'next/link';
import {getSessionUser} from '@/lib/auth';
import {prisma} from '@/lib/prisma';

function money(kobo:number){return `₦${(kobo/100).toLocaleString('en-NG')}`}
type Invoice={invoiceNumber:string;organizationId:string;clientServiceId:string|null;description:string;amountKobo:number;status:string;dueDate:string;createdAt:string;organizationName?:string;installmentIndex?:number;installmentCount?:number};

export default async function CompanyInvoices(){
  const u=await getSessionUser();
  if(!u||!['COMPANY_ADMIN','COMPANY_STAFF'].includes(u.role))redirect('/login');
  const [created,changes,orgs,services]=await Promise.all([
    prisma.auditLog.findMany({where:{entity:'Invoice',action:'INVOICE_CREATED'},orderBy:{createdAt:'desc'},take:100}),
    prisma.auditLog.findMany({where:{entity:'Invoice',action:'INVOICE_STATUS_CHANGED'},orderBy:{createdAt:'asc'},take:500}),
    prisma.organization.findMany({orderBy:{name:'asc'}}),
    prisma.clientService.findMany({where:{status:'ACTIVE'},include:{organization:true,service:true},orderBy:{updatedAt:'desc'}})
  ]);
  const statusMap=new Map<string,string>();
  for(const x of changes){const m=(x.metadata||{}) as Record<string,unknown>;if(typeof m.invoiceNumber==='string'&&typeof m.status==='string')statusMap.set(m.invoiceNumber,m.status)}
  const invoices:Invoice[]=created.map(x=>{const m=(x.metadata||{}) as Record<string,unknown>;const invoiceNumber=String(m.invoiceNumber||x.entityId||'');const organizationId=String(m.organizationId||'');const org=orgs.find(o=>o.id===organizationId);return {invoiceNumber,organizationId,clientServiceId:typeof m.clientServiceId==='string'?m.clientServiceId:null,description:String(m.description||''),amountKobo:Number(m.amountKobo||0),status:statusMap.get(invoiceNumber)||String(m.status||'ISSUED'),dueDate:String(m.dueDate||''),createdAt:String(m.createdAt||x.createdAt.toISOString()),organizationName:org?.name||'Unknown organization',installmentIndex:Number(m.installmentIndex||0)||undefined,installmentCount:Number(m.installmentCount||0)||undefined}});
  const outstanding=invoices.filter(x=>x.status==='ISSUED'||x.status==='OVERDUE').reduce((s,x)=>s+x.amountKobo,0);
  const paid=invoices.filter(x=>x.status==='PAID').reduce((s,x)=>s+x.amountKobo,0);
  return <main className="dashboard"><div className="pill">Company Control</div><div className="row"><div><h1>Invoices</h1><p className="muted">Create invoices, track what is due and record payments manually.</p></div><Link className="btn" href="/company/billing">Back to billing</Link></div><div className="dashnav"><Link href="/company">Overview</Link><Link href="/company/clients">Clients</Link><Link href="/company/partners">Partners</Link><Link href="/company/services">Services</Link><Link href="/company/billing">Billing</Link><Link href="/company/invoices">Invoices</Link><Link href="/company/operations">Operations</Link><Link href="/company/support">Support</Link><Link href="/company/control">Control</Link></div><div className="stats"><div className="stat">Invoices<strong>{invoices.length}</strong></div><div className="stat">Outstanding<strong>{money(outstanding)}</strong></div><div className="stat">Recorded paid<strong>{money(paid)}</strong></div><div className="stat">Active services<strong>{services.length}</strong></div></div><div className="card"><h2>Create invoice</h2><p className="muted">Installments are for one-time services. The first installment is due immediately; later installments unlock after the previous one is marked paid.</p><form action="/api/company/invoices" method="post" className="stack"><label>Client<select name="organizationId" required><option value="">Select client</option>{orgs.map(o=><option key={o.id} value={o.id}>{o.name}</option>)}</select></label><label>Service (optional)<select name="clientServiceId"><option value="">Manual / other</option>{services.map(s=><option key={s.id} value={s.id}>{s.organization.name} — {s.service.name} ({s.billingCycle.replace('_',' ')})</option>)}</select></label><label>Description<input name="description" required defaultValue="Service charge"/></label><label>Total amount (kobo)<input name="amountKobo" type="number" min="1" required placeholder="30000000"/></label><label>Payment plan<select name="installmentCount" defaultValue="1"><option value="1">Pay in full — 1 payment</option><option value="2">2 installments</option><option value="3">3 installments</option></select></label><label>First due date<input name="dueDate" type="date" required/></label><button className="btn" type="submit">Issue invoice</button></form></div><div className="card"><h2>Invoice ledger</h2>{invoices.length===0?<p className="muted">No invoices have been issued yet.</p>:<div className="table-wrap"><table><thead><tr><th>Invoice</th><th>Client</th><th>Description</th><th>Amount</th><th>Due</th><th>Status</th><th>Action</th></tr></thead><tbody>{invoices.map(i=><tr key={i.invoiceNumber}><td>{i.invoiceNumber}{i.installmentIndex&&i.installmentCount?<><br/><small>Payment {i.installmentIndex}/{i.installmentCount}</small></>:null}</td><td>{i.organizationName}</td><td>{i.description}</td><td>{money(i.amountKobo)}</td><td>{i.dueDate?new Date(i.dueDate).toLocaleDateString('en-NG'):'—'}</td><td>{i.status}</td><td>{i.status!=='PAID'&&i.status!=='VOID'&&i.status!=='SCHEDULED'?<form action="/api/company/invoices" method="post"><input type="hidden" name="action" value="status"/><input type="hidden" name="invoiceNumber" value={i.invoiceNumber}/><input type="hidden" name="status" value="PAID"/><button className="btn" type="submit">Mark paid</button></form>:null}</td></tr>)}</tbody></table></div>}</div></main>;
}
