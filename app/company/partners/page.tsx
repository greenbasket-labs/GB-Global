import {redirect} from 'next/navigation';import Link from 'next/link';import {getSessionUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';
export default async function Partners(){const u=await getSessionUser();if(!u||u.role!=='COMPANY_ADMIN')redirect('/');const [apps,partners]=await Promise.all([prisma.partnerApplication.findMany({include:{user:true},orderBy:{createdAt:'desc'}}),prisma.partnerProfile.findMany({include:{user:true,capabilities:true},orderBy:{createdAt:'desc'}})]);return <main className="dashboard"><div className="pill">Company • Partners</div><div className="dashnav"><Link href="/company">Overview</Link><Link href="/company/clients">Clients</Link><Link href="/company/partners">Partners</Link><Link href="/company/services">Services</Link><Link href="/company/billing">Billing</Link><Link href="/company/operations">Operations</Link><Link href="/company/support">Support</Link><Link href="/company/control">Control</Link></div><h1>Partner network.</h1><section className="section"><h2>Applications</h2><div className="stack">{apps.length?apps.map(a=><div className="card" key={a.id}><div className="row"><div><h3>{a.user.name}</h3><p className="muted">{a.user.email}</p></div><span className="badge">{a.status}</span></div><p>{a.capabilities}</p><p className="muted">{a.message||'No message provided.'}</p>{a.status==='PENDING'&&<form action="/api/company/partners/approve" method="post"><input type="hidden" name="applicationId" value={a.id}/><button className="btn" type="submit">Approve partner</button></form>}</div>):<div className="card"><p className="muted">No partner applications.</p></div>}</div></section><section className="section"><h2>Approved partners</h2><div className="grid">{partners.map(p=><div className="card" key={p.id}><div className="row"><div><h3>{p.user.name}</h3><p className="muted">{p.user.email}</p></div><Link className="btn" href={`/company/partners/${p.id}`}>Manage</Link></div><p>{p.capabilities.map(c=>c.name).join(' • ')||'No capabilities'}</p><span className="badge">{p.status}</span></div>)}</div></section></main>}
