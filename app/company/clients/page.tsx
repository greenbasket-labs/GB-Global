import {redirect} from 'next/navigation';import Link from 'next/link';import {getSessionUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';
export default async function Clients(){const u=await getSessionUser();if(!u||!['COMPANY_ADMIN','COMPANY_STAFF'].includes(u.role))redirect('/');const rows=await prisma.organization.findMany({include:{owner:true,_count:{select:{members:true,services:true,applications:true}}},orderBy:{createdAt:'desc'}});return <main className="dashboard"><div className="pill">Company • Clients</div><div className="dashnav"><Link href="/company">Overview</Link><Link href="/company/clients">Clients</Link><Link href="/company/partners">Partners</Link><Link href="/company/services">Services</Link><Link href="/company/support">Support</Link></div><h1>Client organizations.</h1><div className="stack">{rows.map(o=><div className="card" key={o.id}><div className="row"><div><h3>{o.name}</h3><p className="muted">Owner: {o.owner.name} • {o.owner.email}</p></div><span className="badge">{o._count.services} services</span></div><p className="muted">{o._count.members} members • {o._count.applications} applications</p></div>)}</div></main>}
