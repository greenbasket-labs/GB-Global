import {redirect} from 'next/navigation';import Link from 'next/link';import {getSessionUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';
export default async function Client(){const u=await getSessionUser();if(!u)redirect('/login');if(u.role!=='CLIENT')redirect(u.role==='PARTNER'?'/partner':'/company');const o=u.organizations[0]?.organization;if(!o)redirect('/');const [services,apps,tickets]=await Promise.all([prisma.clientService.findMany({where:{organizationId:o.id},include:{service:true}}),prisma.application.findMany({where:{organizationId:o.id}}),prisma.supportTicket.count({where:{userId:u.id,status:{in:['OPEN','IN_PROGRESS']}}})]);return <main className="dashboard"><div className="pill">Client</div><h1>Welcome, {u.name}.</h1><p className="muted">{o.name}</p><div className="dashnav"><Link href="/client">Overview</Link><Link href="/client/services">Services</Link><Link href="/client/applications">Applications</Link><Link href="/client/billing">Billing</Link><Link href="/client/support">Support</Link><Link href="/partner">Partner</Link></div><div className="stats"><div className="stat">Services<strong>{services.length}</strong></div><div className="stat">Applications<strong>{apps.length}</strong></div><div className="stat">Open support<strong>{tickets}</strong></div><div className="stat">Account<strong>Active</strong></div></div><section className="section"><h2>Your services</h2><div className="grid">{services.length?services.map(s=><div className="card" key={s.id}><div className="pill">{s.status}</div><h3>{s.service.name}</h3><p className="muted">Billing: {s.billingCycle}</p></div>):<div className="card"><h3>No services yet</h3><p className="muted">Browse the service catalog to tell Green Basket what you need.</p><Link className="btn" href="/client/services">Browse services</Link></div>}</div></section></main>}
