import {redirect} from 'next/navigation';import Link from 'next/link';import {getSessionUser} from '@/lib/auth';import {getCurrentOrganization} from '@/lib/organization';import {prisma} from '@/lib/prisma';
export default async function Applications(){const u=await getSessionUser();if(!u)redirect('/login');if(u.role!=='CLIENT')redirect(u.role==='PARTNER'?'/partner':'/company');const org=await getCurrentOrganization(u);if(!org)redirect('/client/account');const apps=await prisma.application.findMany({where:{organizationId:org.id},orderBy:{createdAt:'desc'}});return <main className="dashboard"><div className="pill">Applications</div><h1>Your applications.</h1><p className="muted">Software and digital products managed by Green Basket for {org.name}.</p><div className="dashnav"><Link href="/client">Overview</Link><Link href="/client/services">Services</Link><Link href="/client/applications">Applications</Link><Link href="/client/billing">Billing</Link><Link href="/client/support">Support</Link><Link href="/client/account">Account</Link></div><div className="stack">{apps.length?apps.map(a=><div className="card" key={a.id}><div className="row"><div><h3>{a.name}</h3><p className="muted">{a.productName||'Managed application'}</p></div><span className="badge">{a.status}</span></div>{a.url&&<a href={a.url} target="_blank" rel="noreferrer">Open application →</a>}</div>):<div className="card"><h3>No applications yet</h3><p className="muted">When Green Basket provisions an application for this organization, it will appear here.</p></div>}</div></main>}