'use client';
import {useState} from 'react';
export default function TicketReply({ticketId}:{ticketId:string}){const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function send(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/client/support/reply',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ticketId,message})});const d=await r.json();if(!r.ok)setError(d.error||'Could not reply.');else{setMessage('');location.reload()}setBusy(false)}return <form onSubmit={send} className="stack"><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Reply to Green Basket" required rows={4}/>{error&&<p className="error">{error}</p>}<button className="btn" disabled={busy}>{busy?'Sending…':'Reply'}</button></form>}
